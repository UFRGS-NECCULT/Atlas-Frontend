import styled from "styled-components";

export const MapIndex = styled.div`
  .index-content {
    display: flex;
    flex-direction: row;
    justify-content: space-evenly;

    margin: 0 auto;
    width: 100%;

    ul {
      border-left: 3px solid #6dbfc9;
      padding-left: 20px;
      list-style: none;
    }

    a {
      color: #5b5b5f;
    }
  }
`;
